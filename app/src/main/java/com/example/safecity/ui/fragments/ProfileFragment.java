/**
 * ATTENTION: FICHIER DE TEST / PLACEHOLDER TEMPORAIRE (Tâche C6)
 * ⚠️ Ce fichier NE CONTIENT PAS la logique finale de gestion du profil utilisateur.
 * Il sert UNIQUEMENT de conteneur pour résoudre les erreurs de compilation
 * (Cannot find symbol) causées par l'utilisation dans MainActivity.java.
 * * Ce fragment sera remplacé par la logique complète de C6
 * (Affichage des infos A4, Mise à jour A4, Paramètres de notifications B5/B7).
 */
package com.example.safecity.ui.fragments;

import androidx.fragment.app.Fragment;
import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import com.example.safecity.R;

// Fragment temporaire pour résoudre l'erreur de compilation
public class ProfileFragment extends Fragment {

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
        // Remplacer par le layout du profil (C6)
        return inflater.inflate(R.layout.fragment_profile, container, false);
    }
}