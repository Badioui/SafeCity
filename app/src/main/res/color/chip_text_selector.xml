<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Quand sélectionné, on utilise la couleur de carte (blanc ou noir selon mode) -->
    <item android:color="@color/bg_card" android:state_checked="true"/>
    <!-- Sinon, texte principal sémantique -->
    <item android:color="@color/text_primary"/>
</selector>