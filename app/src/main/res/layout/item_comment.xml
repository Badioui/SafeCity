<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <!-- Avatar de l'auteur du commentaire -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/img_comment_profile"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_profile"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.MaterialComponents.SmallComponent"
        app:strokeColor="#F0F0F0"
        app:strokeWidth="1dp" />

    <!-- Bulle de commentaire style "Premium" -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_comment_bubble"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        app:cardCornerRadius="18dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        android:backgroundTint="#F2F3F5"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/img_comment_profile"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="14dp"
            android:paddingVertical="10dp">

            <TextView
                android:id="@+id/tv_comment_username"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#212121"
                android:textSize="13sp"
                android:textStyle="bold"
                tools:text="Jean Dupont" />

            <TextView
                android:id="@+id/tv_comment_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="#424242"
                android:textSize="14sp"
                android:lineSpacingExtra="2dp"
                tools:text="C'est vraiment dangereux à cet endroit, merci du signalement ! J'espère que les autorités vont intervenir rapidement." />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Date de publication sous la bulle -->
    <TextView
        android:id="@+id/tv_comment_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginStart="4dp"
        android:alpha="0.5"
        android:textColor="@android:color/black"
        android:textSize="11sp"
        app:layout_constraintStart_toStartOf="@id/card_comment_bubble"
        app:layout_constraintTop_toBottomOf="@id/card_comment_bubble"
        tools:text="il y a 5 min" />

</androidx.constraintlayout.widget.ConstraintLayout>